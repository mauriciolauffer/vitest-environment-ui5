{
  "name": "test-env-ui5",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "vitest-environment-ui5": "link:../.."
  }
}
